/* import { ProgramService } from "./program.service"

describe("add proram", ()=>{
    const program = new ProgramService
    it("should not create program if user is not admin", ()=>{
        expect (
            program.addProgram(
                {id:1, title:"test", description:" not important", deadline:new Date}, 
                {title:"tarh1", description:"not important", },
                {username:"number1", password:"number2", role:"Representative"})
        ).toBe(false)
    })
}) */